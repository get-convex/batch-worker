"use client";

// The worker component runs entirely on the backend — there are no
// React-specific hooks to export. Use `useQuery(api.<yourModule>.workerStatus)`
// in your app if you want to surface the worker's status in the UI.

export {};
